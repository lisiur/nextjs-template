---
phase: 03-rbac-menu-visibility
plan: 02
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - apps/admin/src/app/(logged)/roles/menus/page.tsx
  - apps/admin/src/app/(logged)/roles/menus/components/role-menu-tree.tsx
  - apps/admin/messages/en.json
  - apps/admin/messages/zh.json
autonomous: true
requirements:
  - RBAC-01

must_haves:
  truths:
    - "Admin can select a role from a dropdown and see the full menu tree with checkboxes"
    - "Currently assigned menus are pre-checked when a role is selected"
    - "Checking a parent menu automatically checks all descendant menus (D-03)"
    - "Unchecking a parent menu automatically unchecks all descendant menus"
    - "Saving assignments sends PUT /menu-role/batch and shows success/error toast"
  artifacts:
    - path: "apps/admin/src/app/(logged)/roles/menus/page.tsx"
      provides: "Role-menu assignment page with role selector and save button"
      contains: "RoleMenusPage"
    - path: "apps/admin/src/app/(logged)/roles/menus/components/role-menu-tree.tsx"
      provides: "Checkbox tree component for menu assignment with auto-include children"
      exports: ["RoleMenuTree"]
    - path: "apps/admin/messages/en.json"
      provides: "English i18n strings for RoleMenus section"
      contains: "RoleMenus"
    - path: "apps/admin/messages/zh.json"
      provides: "Chinese i18n strings for RoleMenus section"
      contains: "RoleMenus"
  key_links:
    - from: "apps/admin/src/app/(logged)/roles/menus/page.tsx"
      to: "appClient.api.menu-role.batch.$put"
      via: "Hono RPC client call on save"
      pattern: "menu-role.*batch.*put"
    - from: "apps/admin/src/app/(logged)/roles/menus/page.tsx"
      to: "appClient.api.menu-role[\":roleId\"].$get"
      via: "Hono RPC client call to load current assignments"
      pattern: "menu-role.*roleId.*get"
    - from: "apps/admin/src/app/(logged)/roles/menus/components/role-menu-tree.tsx"
      to: "apps/admin/src/app/(logged)/applications/[id]/menus/components/menu-tree.tsx"
      via: "Reuse buildTree() and getIcon() patterns"
      pattern: "buildTree|getIcon"
---

<objective>
Build the role-menu assignment admin page: a role selector dropdown, a checkbox tree showing all menus, auto-include children on check, and a save button that persists assignments via the API.

Purpose: Admin UI for RBAC-01 — lets admins control which menus each role can see. This is the management interface that writes MenuRole records consumed by Plan 03's sidebar.

Output: New page at `/roles/menus`, a checkbox tree component, and i18n strings.
</objective>

<execution_context>
@/Users/lisiur/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/lisiur/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-rbac-menu-visibility/03-CONTEXT.md
@.planning/phases/03-rbac-menu-visibility/03-RESEARCH.md
@.planning/phases/03-rbac-menu-visibility/03-UI-SPEC.md
@.planning/phases/03-rbac-menu-visibility/03-01-SUMMARY.md
@apps/admin/src/app/(logged)/roles/page.tsx
@apps/admin/src/app/(logged)/roles/components/role-table.tsx
@apps/admin/src/app/(logged)/applications/[id]/menus/components/menu-tree.tsx
@apps/admin/src/lib/api/index.ts
@apps/admin/src/lib/api/menu.ts
@apps/admin/messages/en.json
@apps/admin/messages/zh.json

<interfaces>
<!-- Key types and contracts the executor needs -->

From apps/admin/src/lib/api/menu.ts (Menu type):
```typescript
export interface Menu {
  id: string;
  appId: string;
  parentId?: string | null;
  name: string;
  code: string;
  icon?: string | null;
  url?: string | null;
  sortOrder: number;
  isExternal: boolean;
  isVisible: boolean;
  createdAt: string;
  updatedAt: string;
}
```

From apps/admin/src/lib/api/app-client.ts:
```typescript
import { hc } from "hono/client";
import type { AppType } from "@/app/api/[[...route]]/route";
export const appClient = hc<AppType>("");
```

From apps/admin/src/app/(logged)/applications/[id]/menus/components/menu-tree.tsx (reusable patterns):
```typescript
// Icon resolution (lines 43-49)
const iconsRecord = icons as Record<string, LucideIcon>;
function getIcon(icon: string | null): React.ReactNode | undefined {
  if (!icon) return undefined;
  const IconComponent = iconsRecord[icon];
  if (IconComponent) return <IconComponent className="h-4 w-4" />;
  return <Folder className="h-4 w-4" />;
}

// Flat-to-tree conversion (lines 52-92)
function buildTree(menus: Menu[]): SortableMenuItem[] {
  const map = new Map<string, SortableMenuItem>();
  const roots: SortableMenuItem[] = [];
  for (const menu of menus) {
    map.set(menu.id, { id: menu.id, name: menu.name, children: [], ... });
  }
  for (const menu of menus) {
    const node = map.get(menu.id)!;
    if (menu.parentId) {
      const parent = map.get(menu.parentId);
      if (parent) parent.children.push(node);
      else roots.push(node);
    } else {
      roots.push(node);
    }
  }
  return roots;
}
```

From apps/admin/messages/en.json (i18n structure):
```json
{
  "Sidebar": { "dashboard": "Dashboard", ... },
  "Roles": { "title": "Role Management", ... }
}
```

From apps/admin/src/app/(logged)/roles/components/role-table.tsx (role IDs):
```typescript
const roles: RoleInfo[] = [
  { id: "admin", name: "admin", ... },
  { id: "manager", name: "manager", ... },
  { id: "user", name: "user", ... },
];
```

From apps/admin/src/components/ui/sidebar (shadcn sidebar components):
```typescript
import { Sidebar, SidebarContent, SidebarFooter, SidebarMenu, SidebarMenuButton, SidebarMenuItem } from "@/components/ui/sidebar";
```

UI-SPEC.md layout contract:
```
Left Panel (1/3): Role selector dropdown
Right Panel (2/3): Menu tree with checkboxes + save button
```

UI-SPEC.md checkbox behavior:
- Check parent → all descendants checked (D-03)
- Uncheck parent → all descendants unchecked
- Check child without parent → parent remains unchecked
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create checkbox tree component and i18n strings</name>
  <files>apps/admin/src/app/(logged)/roles/menus/components/role-menu-tree.tsx, apps/admin/messages/en.json, apps/admin/messages/zh.json</files>

  <read_first>
    - apps/admin/src/app/(logged)/applications/[id]/menus/components/menu-tree.tsx (buildTree, getIcon, SortableNode patterns — lines 43-199)
    - apps/admin/src/lib/api/menu.ts (Menu interface)
    - apps/admin/messages/en.json (existing i18n structure, Sidebar section)
    - apps/admin/messages/zh.json (existing i18n structure, Sidebar section)
    - apps/admin/src/components/ui/checkbox (shadcn checkbox component)
  </read_first>

  <action>
    Create `apps/admin/src/app/(logged)/roles/menus/components/role-menu-tree.tsx`:
    - "use client" directive at top
    - Import `icons` and `LucideIcon` from `lucide-react`, `Folder` and `FolderOpen` from `lucide-react`
    - Import `Checkbox` from `@/components/ui/checkbox`
    - Import `useTranslations` from `next-intl`
    - Reuse the `getIcon()` function from menu-tree.tsx (copy the pattern: iconsRecord lookup, Folder fallback, h-4 w-4)
    - Define a `TreeNode` interface: `{ id: string; name: string; icon?: React.ReactNode; children: TreeNode[] }`
    - Reuse the `buildTree()` function from menu-tree.tsx but simplified (no dnd-kit, no SortableNode — just checkboxes)
    - Props interface `RoleMenuTreeProps`: `{ menus: Menu[]; checkedIds: Set<string>; onCheckedChange: (checkedIds: Set<string>) => void }`
    - Component renders a recursive tree:
      - Each node has: expand/collapse toggle (if hasChildren), icon, checkbox, name label
      - Checkbox checked state: `checkedIds.has(node.id)`
      - On check: collect all descendant IDs (recursive helper), add them + the node itself to the set, call onCheckedChange
      - On uncheck: collect all descendant IDs, remove them + the node from the set, call onCheckedChange
      - Indentation: `paddingLeft: level * 16 + 8` px (matching menu-tree.tsx line 148)
      - Expand/collapse: track expandedIds in local state, default all expanded
    - Export `RoleMenuTree` as named export

    Update `apps/admin/messages/en.json`:
    - Add `"RoleMenus"` key at the same level as `"Sidebar"`, `"Roles"`, etc.
    - Keys: `title` ("Role Menu Assignment"), `description` ("Select a role to manage its menu visibility"), `selectRole` ("Select Role"), `selectRolePlaceholder` ("Choose a role..."), `save` ("Save Assignment"), `saved` ("Menu assignments saved successfully"), `loadError` ("Failed to load menus. Please retry."), `saveError` ("Failed to save assignment. Please retry."), `noRoleSelected` ("Select a role from the dropdown to manage its menu assignments"), `noMenus` ("No menus available. Create menus in the menu management page first")

    Update `apps/admin/messages/zh.json`:
    - Add `"RoleMenus"` key with Chinese translations: `title` ("角色菜单分配"), `description` ("选择角色管理其菜单可见性"), `selectRole` ("选择角色"), `selectRolePlaceholder` ("选择一个角色..."), `save` ("保存分配"), `saved` ("菜单分配已保存"), `loadError` ("加载菜单失败，请重试"), `saveError` ("保存失败，请重试"), `noRoleSelected` ("从下拉菜单中选择一个角色来管理其菜单分配"), `noMenus` ("暂无菜单，请先在菜单管理页面创建菜单")
  </action>

  <verify>
    <automated>grep -c "export function RoleMenuTree" apps/admin/src/app/\(logged\)/roles/menus/components/role-menu-tree.tsx && grep -c "RoleMenus" apps/admin/messages/en.json</automated>
  </verify>
  <acceptance_criteria>
    - `apps/admin/src/app/(logged)/roles/menus/components/role-menu-tree.tsx` exports `RoleMenuTree` component
    - RoleMenuTree renders checkboxes (shadcn Checkbox) for each tree node
    - Checking a parent adds all descendants to checkedIds (auto-include children, per D-03)
    - Unchecking a parent removes all descendants from checkedIds
    - getIcon() resolves icon strings to Lucide components (same pattern as menu-tree.tsx)
    - buildTree() converts flat Menu[] to nested TreeNode[] (same logic as menu-tree.tsx)
    - `apps/admin/messages/en.json` contains "RoleMenus" key with all required sub-keys
    - `apps/admin/messages/zh.json` contains "RoleMenus" key with all required sub-keys
    - Both JSON files are valid JSON (parseable)
  </acceptance_criteria>
  <done>Checkbox tree component created with auto-include children logic, i18n strings added for RoleMenus</done>
</task>

<task type="auto">
  <name>Task 2: Create role-menu assignment page</name>
  <files>apps/admin/src/app/(logged)/roles/menus/page.tsx</files>

  <read_first>
    - apps/admin/src/app/(logged)/roles/menus/components/role-menu-tree.tsx (RoleMenuTree component just created)
    - apps/admin/src/app/(logged)/roles/components/role-table.tsx (role definitions — admin, manager, user with IDs)
    - apps/admin/src/lib/api/app-client.ts (appClient setup)
    - apps/admin/src/lib/api/index.ts (barrel exports)
    - apps/admin/src/app/(logged)/applications/[id]/menus/page.tsx (page pattern reference)
    - apps/admin/src/components/ui/select (shadcn Select component)
    - apps/admin/src/components/ui/button (shadcn Button component)
    - apps/admin/src/components/ui/skeleton (shadcn Skeleton component)
  </read_first>

  <action>
    Create `apps/admin/src/app/(logged)/roles/menus/page.tsx`:
    - "use client" directive
    - Import: `useTranslations` from `next-intl`, `useCallback, useEffect, useMemo, useState` from `react`, `toast` from `sonner`, `appClient` from `@/lib/api`, `RoleMenuTree` from `./components/role-menu-tree`, `Menu` type from `@/lib/api/menu`, shadcn components: `Select, SelectContent, SelectItem, SelectTrigger, SelectValue` from `@/components/ui/select`, `Button` from `@/components/ui/button`, `Skeleton` from `@/components/ui/skeleton`

    State management:
    - `selectedRoleId: string | null` — currently selected role
    - `allMenus: Menu[]` — all menus from GET /menu (flat list)
    - `assignedMenuIds: Set<string>` — menu IDs assigned to the selected role
    - `loading: boolean` — loading state for menus
    - `saving: boolean` — saving state

    Data fetching:
    - On mount: fetch all menus via `appClient.api.menu.$get({ query: { appId } })` — need to pick an appId. Since menus are per-app, but role-menu assignment is global, fetch ALL menus across all apps. If the API requires appId, iterate over apps or use a dedicated endpoint. For now, use `appClient.api.applications.$get()` to list apps, then for each app fetch menus. OR simpler: the batch assign endpoint can accept any menuIds, so just fetch menus from the first app. Actually, looking at the design, the assignment should work across all apps. Fetch all applications first, then for each application fetch its menus, combine them all.
    - On roleId change: fetch assigned menus via `appClient.api.menu-role[":roleId"].$get({ param: { roleId: selectedRoleId } })`, extract the menu IDs into `assignedMenuIds` Set

    Save handler:
    - `handleSave`: call `appClient.api.menu-role.batch.$put({ json: { roleId: selectedRoleId, menuIds: Array.from(assignedMenuIds) } })`
    - On success: `toast.success(t("saved"))`
    - On error: `toast.error(t("saveError"))`

    Layout (per UI-SPEC.md):
    - Container: `container mx-auto py-8`
    - Page header: h1 with `t("title")`, paragraph with `t("description")`
    - Left-right split: flex layout, left panel 1/3 (w-1/3), right panel 2/3 (w-2/3)
    - Left panel: Select component for role selection. Hardcode the three roles: admin (id="admin", name="Administrator"), manager (id="manager", name="Manager"), user (id="user", name="User"). Use shadcn Select with SelectTrigger showing `t("selectRolePlaceholder")` and SelectItem for each role.
    - Right panel: if no role selected, show `t("noRoleSelected")` empty state. If loading, show Skeleton placeholders. If loaded, show RoleMenuTree component + Save button.
    - Save button: `<Button onClick={handleSave} disabled={saving}>{saving ? t("saving") : t("save")}</Button>` — note: use `t("save")` not `t("saving")` text, the saving text comes from a key. Actually add a "saving" key to i18n if not present.

    Route: Next.js App Router auto-generates route from file path. `apps/admin/src/app/(logged)/roles/menus/page.tsx` → `/roles/menus`.
  </action>

  <verify>
    <automated>test -f apps/admin/src/app/\(logged\)/roles/menus/page.tsx && grep -c "RoleMenus" apps/admin/src/app/\(logged\)/roles/menus/page.tsx</automated>
  </verify>
  <acceptance_criteria>
    - Page renders at route `/roles/menus` (file exists at correct path)
    - Role dropdown shows three options: admin, manager, user
    - Selecting a role loads that role's assigned menus from GET /menu-role/:roleId
    - Menu tree renders with checkboxes, assigned menus are pre-checked
    - Save button calls PUT /menu-role/batch with roleId and menuIds array
    - Success toast shows on save, error toast shows on failure
    - Loading state shows Skeleton while fetching
    - Empty state shows when no role is selected
    - Page uses `useTranslations("RoleMenus")` for all user-facing text
  </acceptance_criteria>
  <done>Role-menu assignment page complete with role selector, checkbox tree, save functionality, and i18n</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| admin → API (batch assign) | Admin-only; role from session, not client |
| UI → API (get assigned menus) | Read-only; displays current state for admin review |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-3-04 | Tampering | role-menu assignment | mitigate | Save sends only roleId + menuIds; API validates admin role server-side |
| T-3-05 | Information Disclosure | role menu list | accept | Admin can see all role assignments — intended behavior for admin UI |
</threat_model>

<verification>
- Page file exists at `apps/admin/src/app/(logged)/roles/menus/page.tsx`
- Tree component exports `RoleMenuTree`
- i18n files contain RoleMenus keys
- TypeScript compiles: `pnpm tsc --noEmit` passes
</verification>

<success_criteria>
- Role-menu assignment page accessible at /roles/menus
- Checkbox tree renders all menus with correct hierarchy
- Auto-include children works (check parent → all descendants checked)
- Save persists to API and shows feedback
- All text uses i18n translations
</success_criteria>

<output>
Create `.planning/phases/03-rbac-menu-visibility/03-02-SUMMARY.md` when done
</output>
