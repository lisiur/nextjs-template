---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/service/prisma/schema.prisma
autonomous: true
requirements:
  - APP-01
  - APP-02
  - APP-03
  - APP-04
  - APP-05

must_haves:
  truths:
    - "Application, Menu, and MenuRole models exist in Prisma schema with correct fields and relations"
    - "Database schema is pushed to PostgreSQL and Prisma client is regenerated"
    - "Schema supports soft delete (deletedAt), code uniqueness, menu tree nesting (parentId), and role-menu assignment"
  artifacts:
    - path: "packages/service/prisma/schema.prisma"
      provides: "Application, Menu, MenuRole models with relations and indexes"
      contains: "model Application"
  key_links:
    - from: "packages/service/prisma/schema.prisma"
      to: "PostgreSQL database"
      via: "pnpm db:push"
      pattern: "model Application"
---

<objective>
Create the Prisma schema foundation: Application, Menu, and MenuRole models with all fields, relations, indexes, and constraints needed for the entire application-menu management system.

Purpose: Establishes the data model that all subsequent API and UI work depends on. Menu and MenuRole models are created now (even though their CRUD is Phase 2) to avoid a separate migration later.

Output: Updated `schema.prisma` with three new models, database schema pushed to PostgreSQL, Prisma client regenerated.
</objective>

<execution_context>
@/Users/lisiur/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/lisiur/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-PATTERNS.md
@.planning/phases/01-foundation/SKELETON.md
@packages/service/prisma/schema.prisma

<interfaces>
<!-- Existing schema conventions the executor must follow: -->

From packages/service/prisma/schema.prisma (key patterns):
- Generator: `generator client { provider = "prisma-client" output = "./generated/prisma" }`
- Datasource: `datasource db { provider = "postgresql" }`
- ID pattern: `@id @default(cuid())`
- Timestamps: `@default(now())` for createdAt, `@updatedAt` for updatedAt
- Table naming: `@@map("snake_case")`
- Unique: `@@unique([field])`
- Index: `@@index([field])`
- Relations: explicit `@relation(fields: [...], references: [id], onDelete: Cascade)`
- Self-referential: named relation `"MenuTree"` for parent/children
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Application, Menu, and MenuRole models to Prisma schema</name>
  <files>packages/service/prisma/schema.prisma</files>
  <read_first>
    - packages/service/prisma/schema.prisma (current state — append after Upload model at line 157)
    - .planning/phases/01-foundation/01-RESEARCH.md (lines 140-158 for Application model, lines 156-169 for Menu model, lines 171-177 for MenuRole model)
    - .planning/phases/01-foundation/01-PATTERNS.md (lines 29-103 for schema conventions)
  </read_first>
  <action>
Append three new models after the existing `Upload` model (line 157) in `packages/service/prisma/schema.prisma`. Follow exact conventions from the existing User/Organization models:

**Application model** (per D-01: code is free-form string; per D-03: logo is nullable text for base64; per D-04: deletedAt for soft delete):
- Fields: `id` (String, cuid), `name` (String), `code` (String), `description` (String?), `logo` (String?), `sortOrder` (Int, default 0), `deletedAt` (DateTime?), `createdAt` (DateTime, default now()), `updatedAt` (DateTime, @updatedAt)
- Relation: `menus Menu[]`
- Constraints: `@@unique([code])`, `@@index([deletedAt])`, `@@map("application")`

**Menu model** (supports multi-level nesting per Phase 2 needs):
- Fields: `id` (String, cuid), `appId` (String), `parentId` (String?), `name` (String), `code` (String), `icon` (String?), `url` (String?), `sortOrder` (Int, default 0), `isExternal` (Boolean, default false), `isVisible` (Boolean, default true), `createdAt` (DateTime, default now()), `updatedAt` (DateTime, @updatedAt)
- Relations: `app Application` (FK appId → Application.id, onDelete: Cascade), `parent Menu?` (named relation "MenuTree", FK parentId → Menu.id, onDelete: Cascade), `children Menu[]` (named relation "MenuTree"), `menuRoles MenuRole[]`
- Constraints: `@@index([appId])`, `@@index([parentId])`, `@@map("menu")`

**MenuRole model** (for Phase 3 role-menu assignment):
- Fields: `id` (String, cuid), `menuId` (String), `roleId` (String), `createdAt` (DateTime, default now())
- Relations: `menu Menu` (FK menuId → Menu.id, onDelete: Cascade)
- Constraints: `@@unique([menuId, roleId])`, `@@index([menuId])`, `@@index([roleId])`, `@@map("menu_role")`
  </action>
  <verify>
    <automated>grep -c "model Application" packages/service/prisma/schema.prisma && grep -c "model Menu " packages/service/prisma/schema.prisma && grep -c "model MenuRole" packages/service/prisma/schema.prisma</automated>
  </verify>
  <acceptance_criteria>
    - packages/service/prisma/schema.prisma contains `model Application` with fields: id, name, code, description, logo, sortOrder, deletedAt, createdAt, updatedAt, menus
    - packages/service/prisma/schema.prisma contains `model Menu` with fields: id, appId, parentId, name, code, icon, url, sortOrder, isExternal, isVisible, createdAt, updatedAt, menuRoles
    - packages/service/prisma/schema.prisma contains `model MenuRole` with fields: id, menuId, roleId, createdAt
    - Application has `@@unique([code])` and `@@index([deletedAt])`
    - Menu has `@@index([appId])` and `@@index([parentId])`
    - MenuRole has `@@unique([menuId, roleId])`
    - Menu self-relation uses named relation `"MenuTree"`
    - All models use `@@map("snake_case")` table naming
    - All IDs use `@id @default(cuid())`
    - Application.logo is `String?` (nullable, per D-03 base64 approach)
    - Application.deletedAt is `DateTime?` (nullable, per D-04 soft delete)
  </acceptance_criteria>
  <done>Prisma schema has Application, Menu, MenuRole models with all required fields, relations, and constraints</done>
</task>

<task type="auto">
  <name>Task 2: Push schema to database and regenerate Prisma client</name>
  <files>packages/service/prisma/schema.prisma</files>
  <read_first>
    - packages/service/prisma/schema.prisma (verify Task 1 changes are present)
    - AGENTS.md (confirms: "每次修改 schema.prisma 后，必须重新运行 pnpm db:generate 并重启 dev server")
  </read_first>
  <action>
[BLOCKING] Run database schema push and Prisma client generation. This MUST complete successfully before any API or UI work can begin.

Step 1: Push schema to PostgreSQL:
```
pnpm db:push
```
If the push command requires interactive prompts that cannot be suppressed, run:
```
npx prisma db push --accept-data-loss
```
Note: This is an additive migration (new tables only), so `--accept-data-loss` should not cause data loss. If prompted, confirm the push.

Step 2: Regenerate Prisma client:
```
pnpm db:generate
```

Step 3: Verify the generated client includes the new models:
```
grep -c "application" packages/service/generated/prisma/index.js
```
The grep should return a non-zero count, confirming the Application model is in the generated client.
  </action>
  <verify>
    <automated>pnpm db:generate && grep -c "application" packages/service/generated/prisma/index.js</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm db:push` exits 0 (schema pushed to PostgreSQL)
    - `pnpm db:generate` exits 0 (Prisma client regenerated)
    - `packages/service/generated/prisma/index.js` contains references to "application" (confirming Application model is generated)
    - No TypeScript errors when importing from `@repo/service` (Prisma client is valid)
  </acceptance_criteria>
  <done>Database schema pushed to PostgreSQL, Prisma client regenerated with Application/Menu/MenuRole models available</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Schema → Database | Prisma schema defines data structure pushed to PostgreSQL |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | schema.prisma | accept | Schema changes are code-reviewed via git; no runtime injection vector |
| T-01-02 | Info Disclosure | deletedAt field | accept | Soft-deleted records filtered at API layer (Plan 01-02), not schema layer |
| T-01-SC | Tampering | npm/pip/cargo installs | N/A | No new packages installed in this phase |
</threat_model>

<verification>
- Prisma schema compiles: `npx prisma validate` exits 0
- Schema push succeeds: `pnpm db:push` exits 0
- Client generation succeeds: `pnpm db:generate` exits 0
- Generated client contains new models: `grep -c "application" packages/service/generated/prisma/index.js` returns > 0
</verification>

<success_criteria>
- Application, Menu, MenuRole models exist in schema.prisma with correct fields, relations, and constraints
- Database schema is pushed to PostgreSQL
- Prisma client is regenerated and includes new models
- Schema supports all Phase 1 requirements: soft delete, code uniqueness, base64 logo storage, menu tree nesting, role-menu assignment
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-01-SUMMARY.md` when done
</output>
