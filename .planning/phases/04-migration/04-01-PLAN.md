---
phase: 04-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - packages/service/prisma/seed.ts
autonomous: false
requirements: [MIGR-01]

must_haves:
  truths:
    - "Admin user sees all 7 navigation items in the sidebar when logged in"
    - "Menu items match the current hardcoded navigation (Dashboard, Applications, Organizations, Users, Roles, Settings, Profile)"
    - "Running seed script multiple times does not create duplicate entries"
    - "GET /api/menu/mine returns all 7 menu items for a user with the admin role"
  artifacts:
    - path: "packages/service/prisma/seed.ts"
      provides: "Application, Menu, and MenuRole seeding functions"
      contains: "seedAdminApplication"
  key_links:
    - from: "packages/service/prisma/seed.ts"
      to: "packages/service/prisma/schema.prisma"
      via: "Prisma upsert operations"
      pattern: "prisma\\.application\\.upsert|prisma\\.menu\\.upsert|prisma\\.menuRole\\.upsert"
    - from: "apps/admin/src/stores/menu-store.ts"
      to: "/api/menu/mine"
      via: "fetch in fetchMenus"
      pattern: "appClient\\.api\\.menu\\.mine"
---

<objective>
Add seed data for Application, Menu, and MenuRole tables to populate the database with all current hardcoded admin navigation items.

Purpose: The sidebar already uses dynamic menus from Phase 3, but the database is empty. This plan seeds the actual data so the admin user sees navigation items when logged in.

Output: Updated seed.ts with Application, Menu, and MenuRole seeding functions. Admin user sees 7 menu items in sidebar after running seed.
</objective>

<execution_context>
@/Users/lisiur/.config/opencode/get-shit-done/workflows/execute-plan.md
@/Users/lisiur/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-migration/04-CONTEXT.md
@packages/service/prisma/seed.ts
@packages/service/prisma/schema.prisma
@apps/admin/src/components/layout/sidebar.tsx
@apps/admin/src/stores/menu-store.ts

<interfaces>
<!-- Key types and contracts the executor needs -->

From packages/service/prisma/schema.prisma:
```prisma
model Application {
  id          String    @id @default(cuid())
  name        String
  code        String
  description String?
  logo        String?
  sortOrder   Int       @default(0)
  deletedAt   DateTime?
  createdAt   DateTime  @default(now())
  updatedAt   DateTime  @updatedAt
  menus       Menu[]
  @@unique([code])
}

model Menu {
  id         String   @id @default(cuid())
  appId      String
  app        Application @relation(fields: [appId], references: [id], onDelete: Cascade)
  parentId   String?
  parent     Menu?    @relation("MenuTree", fields: [parentId], references: [id], onDelete: Cascade)
  children   Menu[]   @relation("MenuTree")
  name       String
  code       String
  icon       String?
  url        String?
  sortOrder  Int      @default(0)
  isExternal Boolean  @default(false)
  isVisible  Boolean  @default(true)
  createdAt  DateTime @default(now())
  updatedAt  DateTime @updatedAt
  menuRoles  MenuRole[]
}

model MenuRole {
  id        String   @id @default(cuid())
  menuId    String
  menu      Menu     @relation(fields: [menuId], references: [id], onDelete: Cascade)
  roleId    String
  createdAt DateTime @default(now())
  @@unique([menuId, roleId])
}
```

From packages/service/prisma/seed.ts:
- Current pattern: `prisma.systemConfig.upsert()` with unique constraint for idempotency
- Imports: `PrismaPg` adapter, `PrismaClient` from generated client
- Structure: async `seed()` function, `seed()` called at bottom with catch/finally

From apps/admin/src/components/layout/sidebar.tsx:
- `bottomMenuItems` (lines 24-35): hardcoded footer items (profile, settings) — keep as-is per D-06
- Dynamic menus rendered from `useMenuStore().treeMenus`
- Icon resolution via `iconsRecord[icon]` from lucide-react
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seed Application, Menu, and MenuRole data</name>
  <files>packages/service/prisma/seed.ts</files>
  <read_first>
    - packages/service/prisma/seed.ts (current seed script with SystemConfig pattern)
    - packages/service/prisma/schema.prisma (Menu, Application, MenuRole model definitions)
    - apps/admin/src/components/layout/sidebar.tsx (current sidebar with bottomMenuItems and dynamic menu rendering)
    - apps/admin/messages/en.json (i18n keys for Sidebar — the `code` field must match these keys)
  </read_first>
  <action>
Add three new seeding functions to seed.ts, called after the existing `seed()` function completes:

1. **seedAdminApplication()** — Upsert an Application with code `"admin"`, name `"Admin Panel"`, description `"Administrative dashboard application"`. Use `prisma.application.upsert()` with `where: { code: "admin" }` for idempotency. Store the result (created or found) in a variable for use by menu seeding.

2. **seedMenus(appId: string)** — Upsert 7 Menu items under the admin application. All are top-level (parentId = null). Use `prisma.menu.upsert()` with `where: { id: <stable-cuid> }` for idempotency — generate deterministic IDs using a stable string like `"menu-dashboard"`, `"menu-applications"`, etc. The 7 items per D-03:

   | name | code | icon | url | sortOrder |
   |------|------|------|-----|-----------|
   | Dashboard | dashboard | LayoutDashboard | /dashboard | 0 |
   | Applications | applications | Layers | /applications | 1 |
   | Organizations | organizations | Building2 | /organizations | 2 |
   | Users | users | Users | /users | 3 |
   | Roles | roles | Shield | /roles | 4 |
   | Settings | settings | Settings | /settings | 5 |
   | Profile | profile | User | /profile | 6 |

   The `code` field must match the i18n keys used in `useTranslations("Sidebar")` in sidebar.tsx. Check en.json Sidebar section to confirm exact key names.

   Use stable, deterministic string IDs for idempotency (e.g., `"menu-dashboard"`, `"menu-applications"`, `"menu-organizations"`, `"menu-users"`, `"menu-roles"`, `"menu-settings"`, `"menu-profile"`). These are passed as the `id` field in `prisma.menu.upsert({ where: { id: <stable-id> }, ... })`.

   Set `appId` to the admin application's ID, `isExternal: false`, `isVisible: true`.

3. **seedMenuRoles(menuIds: string[], roleId: string)** — For each menu ID, upsert a MenuRole with `roleId: "admin"`. Use `prisma.menuRole.upsert()` with `where: { menuId_roleId: { menuId, roleId: "admin" } }` for idempotency. This assigns all menus to the admin role per D-04.

Update the main `seed()` function to:
- Call `seedAdminApplication()` first
- Call `seedMenus(adminApp.id)` second
- Call `seedMenuRoles(menuIds, "admin")` third
- Log progress at each step

Keep the existing SystemConfig seeding logic unchanged.
  </action>
  <acceptance_criteria>
    - seed.ts contains functions: seedAdminApplication, seedMenus, seedMenuRoles
    - seedAdminApplication uses prisma.application.upsert with where: { code: "admin" }
    - seedMenus uses prisma.menu.upsert for each of the 7 menu items with stable deterministic IDs (menu-dashboard, menu-applications, etc.)
    - Each menu has correct code matching i18n keys (dashboard, applications, organizations, users, roles, settings, profile)
    - Each menu has correct icon (LayoutDashboard, Layers, Building2, Users, Shield, Settings, User)
    - Each menu has correct url (/dashboard, /applications, /organizations, /users, /roles, /settings, /profile)
    - Each menu has correct sortOrder (0-6)
    - seedMenuRoles uses prisma.menuRole.upsert with roleId: "admin"
    - Main seed() function calls all three new functions in order
    - Running `pnpm --filter @repo/service exec tsx prisma/seed.ts` completes without errors
    - Running the seed script twice does not create duplicate records (idempotent via stable IDs)
  </acceptance_criteria>
  <verify>
    <automated>pnpm --filter @repo/service exec tsx prisma/seed.ts</automated>
  </verify>
  <done>Seed script runs successfully, creates 1 Application, 7 Menus, and 7 MenuRoles. Running again produces no duplicates.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify sidebar renders seeded menu items</name>
  <what-built>
Seed script populates Application, Menu, and MenuRole tables with all 7 admin navigation items. The sidebar component already renders dynamic menus from the database via useMenuStore.
  </what-built>
  <how-to-verify>
1. Run the seed script: `pnpm --filter @repo/service exec tsx prisma/seed.ts`
2. Start the dev server: `pnpm dev`
3. Log in as an admin user (role = "admin")
4. Verify the sidebar shows these 7 items in order:
   - Dashboard (LayoutDashboard icon)
   - Applications (Layers icon)
   - Organizations (Building2 icon)
   - Users (Users icon)
   - Roles (Shield icon)
   - Settings (Settings icon)
   - Profile (User icon)
5. Verify each item navigates to the correct route when clicked
6. Verify the footer still shows Profile and Settings (bottomMenuItems)
7. Verify no duplicate items appear
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| seed.ts → Database | Seed script writes directly to PostgreSQL via Prisma |
| /api/menu/mine → Sidebar | API returns role-filtered menus to frontend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | seed.ts | accept | Seed script runs locally, not exposed to untrusted input |
| T-04-02 | Information Disclosure | Menu data | accept | Menu items are not sensitive — navigation structure only |
| T-04-03 | Elevation of Privilege | MenuRole assignment | accept | "admin" role assignment is correct for admin panel seeding |
</threat_model>

<verification>
After running seed script:
1. `pnpm --filter @repo/service exec tsx prisma/seed.ts` exits 0
2. Database contains 1 Application with code "admin"
3. Database contains 7 Menu records, all with appId pointing to admin application
4. Database contains 7 MenuRole records with roleId "admin"
5. Sidebar renders all 7 items when logged in as admin user
6. Each menu item navigates to the correct route
</verification>

<success_criteria>
1. Admin user sees all 7 navigation items in the sidebar
2. Menu items match the current hardcoded navigation (no missing or extra items)
3. Running seed multiple times does not create duplicates
4. Existing navigation functionality is preserved (no broken links)
</success_criteria>

<output>
Create `.planning/phases/04-migration/04-01-SUMMARY.md` when done
</output>
